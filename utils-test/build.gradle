apply plugin: 'java'
sourceCompatibility = 8

repositories {
	mavenCentral()
}

dependencies {
	compile project(':utils-pdlib'), project(':utils-serialize')
	testCompile group: 'junit', name: 'junit', version: '4.+'
}

sourceSets {
	test {
		java { srcDir 'test' }
		resources { srcDir 'test' }
	}
}